

A FastAPI URL shortener, built as an end-to-end CI/CD exercise on Azure.

What's coming (in order):


FastAPI URL shortener (POST /shorten, GET /{code})
Multi-stage Dockerfile — non-root user, small image
GitHub Actions pipeline: test → build → push to registry → deploy
Deployed live to Azure Container Apps, with health checks and logging


Status: Day 1 — scaffolding. Updated as each piece ships.
